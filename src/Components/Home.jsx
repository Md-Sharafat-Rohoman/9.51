


const Home = () => {
    // const authInfo = useContext(AuthContext);
    // console.log(authInfo);
    return (
        <div>
            <h1>this is home for : </h1>  {/* {authInfo.name} */}
        </div>
    );
};

export default Home;